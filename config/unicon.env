# === UNICON BASELINE (LOCKED) ===
UNICON_PORT=8095

# Cameras (LOCKED roles)
CAM_LEFT=/dev/video0
CAM_RIGHT=/dev/video2
LEFT_ROLE=MEASURE_RAW
RIGHT_ROLE=VIEW_MJPEG

# Notes:
# LEFT may be black in MJPEG -> expected; use RAW/YUYV for compute.
