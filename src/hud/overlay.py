# UNICON: Overlay renderer - placeholder (will draw anchors/lines/text on frames)
# Must be pure: render(frame, state, theme, layout) -> frame

def render(frame, state, theme, layout):
    return frame
